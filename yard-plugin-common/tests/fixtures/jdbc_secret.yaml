job_type: glue
sources:
  - name: users
    source_type: jdbc
    table: public.users
    connection_url: jdbc:postgresql://db.example.com:5432/mydb
    secret_id: prod/db/credentials
sink:
  sink_type: s3
  format: parquet
  path: s3://my-bucket/users-export/
  mode: overwrite
